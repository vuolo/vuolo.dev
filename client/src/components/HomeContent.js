import { Card, Stack } from "@mui/material";
import React, { useState } from "react";

const HomeContent = () => {
  return (
    <Stack spacing={2}>
      <Card></Card>
    </Stack>
  );
};

export default HomeContent;
